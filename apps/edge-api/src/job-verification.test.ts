import { describe, it } from 'vitest'

describe('edge job verification', () => {
    it.todo('rejects invalid signature')
    it.todo('rejects expired job')
    it.todo('rejects replayed nonce')
    it.todo('allows valid signed job')
})
