import { describe, it } from 'vitest'

describe('RBAC guard', () => {
    it.todo('allows when permission present')
    it.todo('denies when permission missing')
    it.todo('super_admin bypasses permission checks')
})
