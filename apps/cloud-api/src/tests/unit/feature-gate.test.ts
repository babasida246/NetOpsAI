import { describe, it } from 'vitest'

describe('feature gate middleware', () => {
    it.todo('denies access when tenant lacks feature')
    it.todo('allows access when tenant has feature')
    it.todo('returns clear error payload for denied access')
})
