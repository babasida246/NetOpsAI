{
    "name": "@config/core",
    "version": "1.0.0",
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsup src/index.ts --format esm --dts --clean",
        "dev": "tsup src/index.ts --format esm --dts --watch",
        "test": "echo 'No tests for config yet'"
    },
    "dependencies": {
        "@contracts/shared": "workspace:*",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "typescript": "^5.3.0",
        "tsup": "^8.0.0",
        "vitest": "^1.0.0"
    }
}