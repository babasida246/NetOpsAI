import { describe, it } from 'vitest'

describe('signed job verification', () => {
    it.todo('verifies signature with public key')
    it.todo('rejects expired jobs based on expires_at')
    it.todo('rejects replayed nonces')
})
